__pycache__/
*.pyc
.venv/
.venv-linux/
venv/
.pytest_cache/
# Only the top-level generated dataset dir — NOT cutoffs/data/ (bundled source
# snapshots that the adapters and catalog build from, which must be committed).
/data/
*.parquet
.env
*.key
secrets.*
