# Pinned to versions with prebuilt Python 3.14 wheels (Python-only stack).
# These are the direct deps. For a fully reproducible install (all transitive
# deps pinned), use the resolved lockfile: pip install -r requirements.lock
# Core data stack
pandas==3.0.3
pyarrow==24.0.0
duckdb==1.5.3
# pandas optional backend needed at startup (pure-Python, not auto-installed):
openpyxl==3.1.5   # read_excel — parse cutoffexamsheet.xlsx (catalog build)
# NOTE: lxml (read_html backend for the on-demand scraper) is intentionally NOT
# pinned here — it needs native compilation and can break wheel-less deploys.
# The scraper degrades gracefully without it. For full local scraping support,
# install from requirements.lock (which pins lxml) instead.

# Fetching (Phase 2+)
httpx==0.28.1
pdfplumber==0.11.9

# Frontend (Phase 3)
streamlit==1.58.0

# Dev / tests
pytest==9.1.0
