# Pinned to versions with prebuilt Python 3.14 wheels (Python-only stack).
# These are the direct deps. For a fully reproducible install (all transitive
# deps pinned), use the resolved lockfile: pip install -r requirements.lock
# Core data stack
pandas==3.0.3
pyarrow==24.0.0
duckdb==1.5.3
# pandas optional backends we rely on (not auto-installed by pandas):
openpyxl==3.1.5   # read_excel — parse cutoffexamsheet.xlsx (catalog build)
lxml==6.1.1       # read_html — generic HTML table scraper

# Fetching (Phase 2+)
httpx==0.28.1
pdfplumber==0.11.9

# Frontend (Phase 3)
streamlit==1.58.0

# Dev / tests
pytest==9.1.0
